import { TppInputDirective } from './tpp-input.directive';

describe('TppInputDirective', () => {
  it('should create an instance', () => {
    const directive = new TppInputDirective();
    expect(directive).toBeTruthy();
  });
});

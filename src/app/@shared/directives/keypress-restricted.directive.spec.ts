import { KeypressRestrictedDirective } from './keypress-restricted.directive';

describe('KeypressRestrictedDirective', () => {
  it('should create an instance', () => {
    const directive = new KeypressRestrictedDirective();
    expect(directive).toBeTruthy();
  });
});

import { TppBtnDirective } from './tpp-btn.directive';

describe('TppBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new TppBtnDirective();
    expect(directive).toBeTruthy();
  });
});
